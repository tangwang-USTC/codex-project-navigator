# Changelog

## 1.0.0

- 项目、普通分组、“未分组”和子分组右键菜单新增“新建 Codex 任务”，通过官方 App Server `thread/start` 创建空 thread，并在原生 Codex 编辑器中打开。
- 项目、分组和子分组右键菜单新增“添加已有任务”，支持按任务名、当前项目、分组、ID 或工作目录搜索并多选归类。
- 项目、分组和子分组右键菜单新增“从本机任务文件夹添加”，可把尚无 Codex 对话的现有目录导入：自动创建 thread、使用文件夹名命名、归入所选层级并打开。
- “新建子分组”始终显示在普通分组右键菜单；当前不是四层模式时自动切换为四层后继续创建。

## 0.9.0

- 首个正式开源版本，在 `tangwang-USTC/codex-project-navigator` 发布源码与可安装 VSIX。
- 确立语义化版本规则：小迭代升级补丁号、小阶段升级次版本号、正式代际升级主版本号。
- 保留中文默认、英文自动加载的单包双语机制，以及二至四层项目/分组导航、事件驱动最近列表和原生 Codex 会话继续能力。
- 增加公开支持、安全报告、贡献指南、CI 与独立项目元数据。

## 0.8.0

- 使用 VS Code 原生 `package.nls` 与 `vscode.l10n` 机制提供中文默认、英文自动切换的单包双语界面。
- 新增 `README.en.md`，GitHub 上由中文主 README 与英文 README 显式互链。
- 新建分组和子分组时自动预填“分组 N / 子分组 N”；英文界面对应 “Group N / Subgroup N”。
- 活动栏图标改为六个独立菱形单元组成的几何标识，与 Codex 的六向结构形成视觉关联但保持独立辨识度。

## 0.7.0

- 新增四层结构：项目 → 分组 → 子分组 → 任务，并兼容既有三层分组状态。
- 新增子分组创建、重命名、删除和任务移动选择；切回三层时自动汇总显示。
- 最近 7 条改为监听 App Server thread/turn 活动和本机 Codex 会话存储变更后立即更新，默认关闭周期轮询。
- 插件说明改为独立产品口径，移除与其他导航插件及内部开发体系相关的表述。

## 0.6.0

- 新增持久化项目顺序，项目可在同层级中上移或下移。
- 新增项目父子关系；拖动项目到另一个项目可建立子项目，拖回“项目”根节点可提升为顶级项目。
- 新增右键“调整项目层级”和“提升为顶级项目”，兼顾鼠标、触屏远程和命令式操作。
- 新增“提升分组为独立项目”；分组内任务只改变 Navigator 归属，原始 thread `cwd` 和对话保持不变。
- 新增项目循环检测和无工作目录自定义项目兼容。

## 0.5.0

- 新增右键“移动到项目/分组”，仅改变 Navigator 层级归属并保留原始 thread `cwd`。
- 新增已归档任务的永久删除入口，调用官方 `thread/delete` 并要求不可恢复确认。
- 最近任务默认改为 7 条，严格按更新时间滚动，置顶任务不再挤占最近列表。
- 新增清晰的右键“置顶任务”和“取消置顶”入口。
- 层级独占模式成为默认，兼容模式保留用于迁移、排障和回退。

## 0.4.0

- 任务点击默认改为在官方 Codex 原生会话编辑器中继续。
- 保留模型选择、审批/确认按钮和原生输入框，改善鼠标与手机远程触屏操作。
- 新增任务行原生对话按钮，以及原生界面/终端/复制 ID/官方侧栏打开方式菜单。
- 原生会话入口不可用时提供终端和复制 ID 安全回退。
- 归档任务在任一继续方式前统一确认恢复。
- 层级独占模式不再依赖终端作为默认会话界面。

## 0.3.0

- 新增层级兼容模式：官方 Codex 视图与层级树同时显示。
- 新增层级独占模式：隐藏官方 Codex View，只保留层级任务树。
- 新增标题栏按钮和命令面板命令，一键切换并持久保存模式。
- 打开官方 Codex 侧栏时自动恢复兼容模式。

## 0.2.0

- 将项目任务树嵌入官方 Codex 主侧栏或辅助侧栏，不再占用独立活动栏入口。
- 添加对官方 `openai.chatgpt` 扩展的显式依赖和侧栏容器兼容检测。
- 保持 App Server 同步、二级/三级分组、搜索、置顶、归档和重命名功能不变。

## 0.1.0

- 首个独立侧栏版本。
- 支持最近、项目、可选三级分组、搜索、置顶、归档、重命名与终端继续任务。
- 通过官方 Codex App Server 同步任务元数据。
