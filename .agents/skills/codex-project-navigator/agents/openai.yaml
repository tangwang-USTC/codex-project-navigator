interface:
  display_name: "Codex Project Navigator"
  short_description: "Repo-specific patterns and workflows for codex-project-navigator"
  default_prompt: "Use the codex-project-navigator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true